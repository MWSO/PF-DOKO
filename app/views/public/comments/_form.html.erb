<%= form_with model: [post_image, comment] do |f| %>
  <div class= "form-group">
    <%= f.label :comment, "コメント入力欄" %>
    <%= f.text_area :comment, row: "5", placeholder: "コメント" %>
    <%= f.submit "送信", class: "btn btn-success" %>
  </div>
<% end %>
